import styled from 'styled-components';
import { palette } from 'styled-tools';

export const Headline = styled.h1`
  color: ${palette('white')};
  font-size: 5rem;
  line-height: 1.2;
  text-align: center;
`;
