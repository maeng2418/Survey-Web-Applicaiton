import styled, { css } from 'styled-components';
import { Paper, Button } from '@material-ui/core';

export const SaveSurveyBtnPaper = styled(Paper)`
  display: flex;
`;

export const SaveSurveyBtn = styled(Button)`
  flex: 1;
`;
