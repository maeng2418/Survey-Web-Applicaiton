import { List as MaterialList } from '@material-ui/core';
import { styled as materialStyled } from '@material-ui/core/styles';

export const List = materialStyled(MaterialList)({
  width: '18rem',
});
